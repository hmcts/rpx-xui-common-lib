import { RemoveDuplicates } from './duplicate-filter.pipe';

describe('Pipe: RemoveDuplicates', () => {
  it('create an instance', () => {
    let pipe = new RemoveDuplicates
    expect(pipe).toBeTruthy();
  });
});
